# 工作任务布置模板（纯文字，直接粘贴即用）

把下面模板**整段复制**，替换示例内容，丢进工作台「事项看板 → 📋 粘贴提取」即可。
系统会**自动识别「事项名称」**，并从正文里抓取「负责人」「时间节点」，**无需手动调整**。

## 模板（复制下面整段，按你的情况替换示例）

```
👉一、工作布置（此行是提示，不会被当成任务）
@张老师  期中教学质量分析    9.9（周三）13:00
@李教务  提交月度报表       下周一 18:00
（王主管）核对新生名单      8月25日前
新生家长会                2026-09-12 19:00
```

## 三个字段怎么识别

| 字段 | 系统怎么识别 | 你能怎么写 |
|---|---|---|
| **事项名称** | 每行去掉负责人、时间后剩下的话，自动当作事项名 | ➜ 直接写"要做什么"，不用单独标「事项名称」 |
| **负责人** | 自动从行内抓取 | `@姓名` / `（姓名）` / `姓名—` 标注；不写就留空 |
| **时间节点** | 自动从行内抓取「日期 + 时刻」 | `2026-09-09`、`9月9日`、`9.9`、`下周三`、`明天`、`9.9 13:00` 都认 |

## 自动识别提示位说明

- 模板首行 `👉一、工作布置…` 是**预留的「事项名称」提示位**：它用 `👉` 章节标题格式，系统会跳过它，**不会变成待办**；它的作用只是提醒你「下面每行就是一条任务、会自动提取事项名」。
- 真正的待办来自下面的**数据行**：每一行 = 一条任务。上面那行提示和空行都会被自动忽略。

## 灵活写法（顺序随意，照样能拆）

```
👉一、本周任务
下周一下班前 找家长面谈 @客服部
9.10 14:00 集团线上培训（全员）
家长会 2026-09-12 19:00 @李老师
```

- 没有负责人 → 留空不报错；没有时间 → 留空不报错。
- 想加优先级，可在行内写 `紧急` / `重要`，系统会标记。

## 想要「固定表格」精确提取？

如果更习惯规整表格，用 `|` 分隔（系统按内置「任务表格模板」规则识别，列顺序保持「事项 | 负责人 | 截止 | 时间 | 优先级」）：

```
事项 | 负责人 |  * 截止日期 * | 时间 | 优先级
专业考 | 张老师 | 2026-09-09 | 13:00 | 重要
```
