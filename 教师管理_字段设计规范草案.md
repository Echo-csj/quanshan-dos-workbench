# 教师管理模板 — Excel 现状分析与字段设计规范（草案 v1）

> 目的：为支撑工作台「教师管理」模块（支持后续 Excel 批量上传更新教师基本信息），先对现有表格做接收、分析，并给出规范的字段设计与数据结构建议。本文件为设计草案，**功能尚未实现**，待确认后进入开发。

## 一、本次接收的文件
- 文件名：`教师周报模板_备份_20260825b.xlsx`
- 实际内容：**教师基本信息表**（非周报），文件名与内容不符，建议重命名。
- 结构：1 个工作表「模板」，共 14 列（A–N），其中 A–I 有数据、J–N 为空；24 条教师记录（第 2–25 行），第 1 行为表头。

## 二、现有字段结构（现状）
| 列 | 字段 | 示例 | 格式/类型 | 说明 |
|---|---|---|---|---|
| A | 序号 | 1 | 数字 | 排序号，非主键 |
| B | 教师 | 王静静 | 文本 | 姓名，可重名，无唯一标识 |
| C | 学科组 | 数学 | 枚举(数学/英语/文综/理综) | 粗粒度分组 |
| D | 岗位 | DOST / 中级教研员 | 文本 | **英文缩写与中文职称混用** |
| E | 入职日期 | 2022-08-16 00:00:00 | 日期时间 | 含多余 00:00:00 |
| F | 工龄 | 4年0月10天 | 文本(派生) | **由入职日期算出，已冻结** |
| G | 毕业院校 | 安徽农业大学 | 文本 | 含"高等院校"等非具体值 |
| H | 专业 | 农业工程 | 文本 | 含" / 辅修"复合值 |
| I | 证书 | 初中数学，高中生物 | 文本(逗号分隔) | 自由文本、中英文逗号混用、"无"占位 |
| J–N | — | 空 | — | 5 个空列，应清理 |

**记录完整性**：24 条记录的姓名/学科组/岗位/入职日期/工龄/毕业院校/专业均填写；证书 5 条为"无"（R3 陈雅文、R13 徐硕阳、R15 蒋文宇、R16 王筱枫、R21 孙周硕），其余至少 1 项。

**分布统计**：
- 学科组：数学 6 / 英语 7 / 文综 8 / 理综 3
- 岗位：TR 7、TRS 5、中级教研员 4、TRM 2、TRMT 2、初级教研员 2、DOST 1、GPS2 1（**英文缩写 18 条 + 中文职称 6 条，两套体系混用**）

## 三、发现的数据格式与质量问题（逐条）
1. **文件名误导**：名为"周报模板"实为"教师基本信息"，易误用。
2. **工龄是派生值且被冻结**：`工龄 = 今天 − 入职日期`，属可计算字段，存成静态文本会随日期变旧、且无法按"截至某日期"统计。应在系统中动态计算，不入库。
3. **岗位编码混用两套体系**：英文缩写（DOST/TRS/TRM/TRMT/GPS2/TR）与中文职称（中级教研员/初级教研员）并存于同一字段，无法统一筛选/统计。**需要一份岗位编码对照表（codebook）**，并统一为受控枚举。
4. **证书为自由文本**：用全角/半角逗号混隔（"初中数学，高中生物"），且以"无"占位；多证书挤在一个单元格，无法检索"持专八人数""持高中物理证人数"。应拆分为**证书列表**（数组），并建受控词表。
5. **无稳定主键**：只有"姓名"，重名时会冲突、批量更新无法可靠匹配。应增加**教师工号/唯一 ID** 作为主键。
6. **序号冗余**：序号可由列表自动生成，不需作为数据列维护；删除/插入行会错位。
7. **空列 J–N**：5 个空列，属模板残留，应删除。
8. **缺常见管理字段**：无性别、出生日期、手机号、邮箱、校区、在职状态（在职/试用/离职）、转正日期、最高学历等，难以支撑完整教师管理。
9. **个别异常值**：毕业院校出现"高等院校"（R4 李悦，疑似未填具体校名）；专业与任教学科可能不一致（如 R4 专业"会计"但证书"小学语文"），属录入不一致，导入时应有校验/提示。
10. **学科组为粗粒度**：文综/理综 是组合，无法区分具体任教学科（语文/数学/英语/物理…）。证书里其实隐含任教学科，建议补「任教学科」字段。

## 四、优化后的字段设计（建议）
按"基础信息 / 任职信息 / 教育背景 / 资质 / 系统"分组：

### 基础信息
- **teacherId**（教师工号，**主键，必填，唯一**）
- name（姓名，必填）
- gender（性别：男/女/未知，枚举）
- birthday（出生日期，日期，可选）
- phone（手机号，可选）
- email（邮箱，可选）
- campus（校区，如"泉山校区"，枚举/文本）

### 任职信息
- subjectGroup（学科组：数学/英语/文综/理综，枚举）— 保留现有粗分组
- teachSubject（任教学科：语文/数学/英语/物理/… 可多选，数组）— 由证书中提炼
- positionCode（岗位编码，受控枚举，**需你提供 codebook**）
- positionTitle（岗位/职称中文名，如 中级教研员，文本）
- entryDate（入职日期，日期，必填）
- regularDate（转正日期，日期，可选）
- status（在职状态：在职/试用/离职/休假，枚举，默认 在职）

### 教育背景
- educationLevel（最高学历：大专/本科/硕士/博士，枚举）
- school（毕业院校，文本）
- major（专业，文本，可含辅修说明）

### 资质
- certificates（证书列表，数组；受控词表如 初中数学/高中生物/专八/…；空=未填写，不用"无"）

### 系统/派生（不入库，计算展示）
- 工龄 = 今天 − entryDate（动态）
- 年龄 = 今天 − birthday（动态）

> 说明：原「序号」改为列表自动序号；原「工龄」改为派生不存储；原「证书」拆为数组；原「岗位」拆为 编码 + 中文名 两字段。

## 五、数据结构与存储建议（面向批量上传功能）
1. **主键与 upsert**：以 `teacherId` 为主键。上传时按 teacherId 匹配：
   - 已存在 → 更新该记录字段（可选"仅更新非空字段"或"整行覆盖"）
   - 不存在 → 新增
   - 缺 teacherId 的行：可回退按 name 匹配（提示风险）或标记"待人工确认"。
2. **数组字段**：certificates / teachSubject 在 Excel 中用分隔符（建议 `、` 或 `|`）填写，导入时拆分为数组；导出时再合并。
3. **受控枚举**：subjectGroup / gender / status / educationLevel / positionCode 建白名单，导入时校验，非法值标红并提示。
4. **校验规则**：必填项（teacherId/name/entryDate）、日期格式（YYYY-MM-DD）、手机号格式、重复主键检测。
5. **导入流程（建议）**：模板下载 → 填写/粘贴 → 上传文件 → **字段映射**（容错表头别名，如"教师"→name）→ **预览差异**（新增 X / 更新 Y / 跳过 Z / 报错 W）→ 用户确认 → 入库（带备份/撤销）。
6. **模板容错**：用户 Excel 表头可能与标准不同，提供"列映射"界面，避免每次都要求严格模板。

## 六、推荐的标准导入模板（列清单）
| 列 | 字段 | 必填 | 格式 | 示例 |
|---|---|---|---|---|
| A | teacherId | 是 | 文本/数字 | T2024001 |
| B | name | 是 | 文本 | 王静静 |
| C | gender | 否 | 男/女/未知 | 女 |
| D | birthday | 否 | YYYY-MM-DD | 1998-03-12 |
| E | phone | 否 | 11位 | 138xxxx |
| F | campus | 否 | 文本 | 泉山校区 |
| G | subjectGroup | 是 | 数学/英语/文综/理综 | 数学 |
| H | teachSubject | 否 | 用 `、` 分隔 | 初中数学、高中生物 |
| I | positionCode | 是 | 受控编码 | TRS |
| J | positionTitle | 否 | 文本 | 中级教研员 |
| K | entryDate | 是 | YYYY-MM-DD | 2022-08-16 |
| L | regularDate | 否 | YYYY-MM-DD | 2022-09-16 |
| M | status | 否 | 在职/试用/离职/休假 | 在职 |
| N | educationLevel | 否 | 大专/本科/硕士/博士 | 本科 |
| O | school | 否 | 文本 | 安徽农业大学 |
| P | major | 否 | 文本 | 农业工程 |
| Q | certificates | 否 | 用 `、` 分隔 | 初中数学、高中生物 |
| R | remark | 否 | 文本 | — |

## 七、待你确认 / 下一步
1. **岗位编码对照表**：请补充 DOST / TRS / TRM / TRMT / GPS2 / TR 及 中级/初级教研员 的实际含义与层级，这是规范化的关键。
2. **字段取舍**：是否纳入 手机号/邮箱/校区/生日/学历 等？还是先做精简版（工号、姓名、学科组、任教学科、岗位、入职日期、证书）？
3. **数据来源**：现有 24 条是否作为初始数据直接导入系统？还是仅作模板示例？
4. 确认后，我再实现「教师管理」模块：模板下载 + Excel 上传 + 字段映射 + 预览差异 + 批量更新入库（含备份/撤销）。
