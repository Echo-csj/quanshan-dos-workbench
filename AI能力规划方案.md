# 状元港·泉山校区 DOS 工作台 · AI 能力规划方案

> 版本 v1.0　|　产出日期 2026-08-29　|　状态：待评审
> 目标：在不破坏现有纯静态架构的前提下，为工作台注入可落地的 AI 能力，形成长期生产力。

---

## 一、现状梳理

### 1.1 系统架构概览

| 维度 | 现状 |
|---|---|
| 形态 | 纯静态前端（HTML + 原生 JS，零框架），GitHub Pages 部署 |
| 全局对象 | `window.App`（`App.store` 数据层 / `App.router` / `App.util` / `App.sync` 等） |
| 本地存储 | `localStorage`，key = `zyg_workbench_v1`（整份 JSON 文档） |
| 云端 | Supabase：`dos_workbench`（整档同步）、`shared_link`（接收分析台快照）、多层级表（org / org_member / share_grant / shared_item / share_log） |
| 关联系统 | 数据分析工作台 campus-analytics 经 Supabase 推送 `analytics_snapshot` 快照（weekly / monthly / bestkezu / kezuActual / tkpi 各 stream 最新记录） |
| 数据模型 | `settings` / `timeline` / `reports.monthly` / `projects` / `tasks` / `hr.weekly` / `teachers` / `teacherMilestones` |

### 1.2 页面核心功能与用户使用流程

| 视图 | 核心功能 | 典型流程 | 主要数据 |
|---|---|---|---|
| 今日指挥台 `/today` | 今日工作筛选、黄历宜忌、今日/本周节律、周报草稿生成 | 看今日待办 → 跳转处理 → 点「周报草稿」生成文本 → 复制微调上报 | tasks / timeline / reports / hr / teacherMilestones |
| 时间轴 `/timeline` | 周/月节律时间线、节点拖拽排程 | 新建节点 → 填星期/日期/颜色/提醒 → 拖拽改期 | timeline.fixedNodes / customNodes |
| 事项看板 `/tasks` | 四列看板、粘贴提取、规则引擎、归档、任务协作 | 新建 /「从时间轴生成」/「粘贴提取」→ 拖拽流转 → 归档 | tasks（title/priority/status/assignee/dueDate/note/scope/projGroup…） |
| 数据看板 `/data` | 基准对标、环比同比、人事数据、🔗联动数据 | 选月份/指标 → 看红绿灯 → 手写文字分析 | reports.monthly / hr / 快照 |
| 联动数据（data 内 Tab） | 快照 weekly 流 7 核心卡 + 6 分组 | 分析台推送 → 登录同步 → 看联动 | shared_link 快照 |
| 科组生产指标 | 最佳科组排名、科组生产预测（G1/G2/G3） | 手动调 C 值 → 选参考月 → 看一致性校验与预测 | 快照 kezu / 本地记忆 dos_kezu_target_C |
| 教师管理 `/teachers` | 花名册、状态标签、里程碑 | 查看/编辑教师 → 维护状态 | teachers / teacherMilestones |
| 项目组中心 `/projects` | 6 项目组 | 查看/维护项目组 | projects |
| 子工作台管理 `/org` | 组织纳管、共享规则、下发/回传、标注 | 纳管成员 → 建共享规则 → 下发 → 合并回传 | org/member/grant/shared_item |
| 设置 `/settings` | 规则引擎、提醒、备份、视图偏好 | 调配置 | settings |

### 1.3 用户核心周节律（业务主线）

```
周一集团会议 → 周二事项拆解/下发/跟进 + 主管会/教务会 → 周日 DOS 周报
```

### 1.4 现状痛点（AI 机会点）汇总

1. **周报半自动**：`generateWeeklyReport` 只罗列完成事项与数字，「进行中/待跟进」是空话一句，第四部分数据解读需人工手写。
2. **粘贴解析遇边界**：规则引擎（`extractionRules`）对低置信度/未覆盖句式无能为力，负责人靠正则匹配易错、无法结合教师库消歧。
3. **数据有数字无解读**：红绿灯/环比同比只出数字，文字分析与整改建议完全靠 DOS 手写。
4. **预测缺解释**：科组生产预测需手动调 C、手动看一致性校验，无 C 值建议、无预测解释。
5. **导入脆弱**：Excel 导入手动选类型、月份靠文件名解析（易错）、模板不一致即失败、缺数据质量校验。
6. **教师管理缺量化与预警**：仅标签维护在职状态，无 tkpi 量化联动、无离职/绩效风险预警。
7. **多层级汇总靠人工**：总台读各子台数据后靠人脑汇总、写评语。
8. **时间轴/提醒偏静态**：节律节点全手动排程，`reminder` 字段无实际触发，无个性化优先级排序。

---

## 二、AI 能力规划总览

### 2.1 技术路线（混合方案 · 推荐）

采用 **「L0 本地智能层」+「L1 LLM 增强层」** 双层架构，兼顾零成本、隐私与强能力：

| 层 | 定位 | 实现方式 | 成本 | 数据出网 |
|---|---|---|---|---|
| **L0 本地智能** | 规则 + 统计算法，随页面即开即用 | 纯 JS（移动平均/指数平滑/线性回归/Z-score 异常/模板化生成/智能提醒） | ¥0，无需 Key | 否 |
| **L1 LLM 增强** | 语义理解、自由文本生成、问答 | OpenAI 兼容大模型 API（DeepSeek / 通义千问等） | 按 token，极低 | 是（可选脱敏） |

**关键设计原则**：所有 AI 能力均以「外挂增强层」注入，**不改变现有数据模型与 localStorage/Supabase 结构**；LLM 调用统一收敛到新模块 `js/ai.js`，便于统一管理 Key、脱敏、降级与限流。

### 2.2 Key 安全与接入方案（关键决策）

纯静态前端不能硬编码 API Key，提供两级可选方案：

- **方案 A（推荐先用）**：用户自填 Key——在「设置 → AI」输入自己的大模型 Key，存 `localStorage`，前端直连。零部署成本、符合「本地优先 + 纯静态」习惯；Key 归用户个人、不进入 Git 仓库。
- **方案 B（后续升级）**：Supabase Edge Function 代理——Key 存服务端环境变量，前端无感知、更安全、可加限流与审计，但需部署与少量配置。

> 隐私兜底：若用户选择「脱敏」，`js/ai.js` 在发送前将教师姓名等替换为代号。

### 2.3 能力地图（功能点 ↔ 对应模块 ↔ 技术层）

| # | AI 功能点 | 落点模块 | 技术层 | 类别 |
|---|---|---|---|---|
| F1 | 周报智能生成 | today.js | L1 | 内容生成 |
| F2 | 粘贴任务语义解析 | tasks.js | L1（L0 兜底） | 语义理解/自动化 |
| F3 | 数据红绿灯智能解读 | data.js | L1 | 内容生成 |
| F4 | 异常检测 + 趋势预测 | data.js / linked-kezu.js | L0 | 数据分析/预测 |
| F5 | 科组 C 值智能建议 + 预测解释 | linked-kezu.js | L0+L1 | 智能推荐 |
| F6 | Excel 导入智能辅助 | importer.js | L0+L1 | 自动化 |
| F7 | 教师智能画像 + 预警 | teachers.js | L0+L1 | 智能推荐/预测 |
| F8 | 项目组智能催办/健康度 | projects.js | L0 | 自动化/智能推荐 |
| F9 | 子台汇总评语自动生成 | org-admin.js / master-hub.js | L1 | 内容生成 |
| F10 | 自然语言问答/智能报表 | 全局（新入口） | L1 | 数据分析 |
| F11 | 时间轴节律智能排程 | timeline.js | L0+L1 | 自动化/智能推荐 |
| F12 | 今日优先级智能排序 + 提醒 | today.js | L0 | 智能推荐 |

---

## 三、具体 AI 功能点设计

> 每个功能点按「定位 → 提升效率 → 实现思路 → 数据/接口条件 → 成本风险」展开。

### F1 周报智能生成（P0，价值最高）

- **定位**：升级 `today.js` 的 `generateWeeklyReport`，从「半自动草稿」到「一键完整周报」。
- **提升效率**：周日周报从 1–2 小时「凑字」缩短到 5 分钟「审阅微调」，且口径一致、不漏项。
- **实现思路**：新增 `App.ai.generateWeeklyReport(aggregated)`，聚合 `tasks`（本周完成/进行中/逾期）、`reports.monthly`（最新月指标）、`hr.weekly`、`linked-kezu`（科组达成）、`teacherMilestones`，调用 LLM 按既定周报四段模板生成正文（完成项 / 进行中与跟进 / 数据对比与异常归因 / 下周计划与整改建议），渲染为可编辑文本 + 一键复制。
- **数据/接口条件**：LLM API（OpenAI 兼容）；聚合函数（现有 `App.viewData` / `readShared` 已够）；周报模板 prompt。
- **成本风险**：低（单次调用）；风险在 prompt 需迭代调优以贴合集团周报格式。

### F2 粘贴任务语义解析（P0）

- **定位**：在 `tasks.js` 现有 `parseWithRule` 规则引擎之上加 LLM 语义兜底。
- **提升效率**：把「周二拆解下发」从半自动（规则未覆盖时逐条手改）变为真一键，复杂群消息也能正确拆多任务、识依赖。
- **实现思路**：规则引擎解析后，对低置信度行/未覆盖行批送 LLM，输出结构化 JSON（`{title,dueDate,assignee,priority,note,parentId,projGroup}`），映射回任务字段；`assignee` 结合 `teachers` 花名册做姓名消歧；自动建议 `projGroup` 标签；同时支持「从历史粘贴学习」沉淀为新规则。
- **数据/接口条件**：LLM API；`teachers` 花名册（消歧词典）；现有 `extractionRules` 结构。
- **成本风险**：中（需约定 JSON 输出 schema + 校验）；离线时优雅降级回纯规则。

### F3 数据红绿灯智能解读（P0）

- **定位**：`data.js` 基准对标、`linked-data.js` 各卡，从「只有红绿灯」到「自动文字归因」。
- **提升效率**：省去 DOS 逐卡盯数字、手写解读；周报第四部分可直接引用。
- **实现思路**：把「达标/临界/异常」卡与环比同比结果组装成结构化清单，LLM 生成简洁归因与整改建议；前端渲染为每卡下方的「AI 解读」折叠条。
- **数据/接口条件**：LLM API；`App.util.judge` 判定结果 + `App.baseline` 基准。
- **成本风险**：低。

### F4 异常检测 + 趋势预测（P0，纯本地零成本）

- **定位**：对 `reports.monthly` 时间序列做突增/突降告警与下期预测。
- **提升效率**：从「人眼发现异常」到「系统自动提示 + 给出下月预判」，提前干预。
- **实现思路**：新增 `js/ai-local.js`，纯 JS 实现——移动平均/指数平滑/线性回归预测下月（离职率、生产完成率、饱和度、单科比等）；Z-score / 阈值检测环比同比异常并标红；结果以「AI 洞察」卡片呈现。
- **数据/接口条件**：仅需 `reports.monthly` 历史数据（已具备），**无任何外部依赖**。
- **成本风险**：¥0；风险极低。

### F5 科组 C 值智能建议 + 预测解释（P0/P1）

- **定位**：`linked-kezu.js` 目前手动调 C、手动看一致性校验。
- **提升效率**：给出 C 值建议区间与预测偏差解释，减少试错。
- **实现思路**：L0 用历史达成率反推 C 建议区间（纯统计）；L1 用 LLM 解释「为何某科组预测高/低」并生成下月指标文字。
- **数据/接口条件**：快照 `kezu` 历史 + 本地 `dos_kezu_target_C` 记忆；L1 需 LLM。
- **成本风险**：低–中。

### F6 Excel 导入智能辅助（P1）

- **定位**：`importer.js` 摆脱手动选类型、文件名猜月份、模板不一致即失败。
- **提升效率**：导入成功率与速度大幅提升，减少重复导入与人工核对。
- **实现思路**：L0 用表头/结构特征自动识别报表类型；L1 对失败行做列级诊断、脏值/口径偏差提示、自动建议字段映射。
- **数据/接口条件**：SheetJS（已有）；LLM（L1 诊断）。
- **成本风险**：低。

### F7 教师智能画像 + 预警（P1）

- **定位**：`teachers.js` 聚合基本信息 + 里程碑 + tkpi 量化表现，生成画像与风险预警。
- **提升效率**：管理者一眼看全教师状态，提前发现离职/绩效风险。
- **实现思路**：新增「教师画像」卡——L0 聚合 tkpi 派生率（续费/结课/退费/停课率、饱和度）、里程碑进度、入离职时间做规则化评分；L1 生成一段综合评语与风险提示。
- **数据/接口条件**：`teachers` + `teacherMilestones` + 快照 `tkpi`（需按姓名匹配，两端姓名需约定一致）；L1 需 LLM。
- **成本风险**：中（需先打通 tkpi 联动 + 姓名匹配键）。

### F8 项目组智能催办/健康度（P1，纯本地）

- **定位**：`projects.js` 6 项目组，按任务完成度/逾期情况给健康度与催办建议。
- **提升效率**：自动识别滞后项目组，一键生成催办清单。
- **实现思路**：L0 统计各项目组任务完成率、逾期数、临期数，输出红黄绿健康度 + 催办名单（可按负责人聚合）。
- **数据/接口条件**：`tasks`（含 `projGroup`）+ `projects`，无外部依赖。
- **成本风险**：¥0；低。

### F9 子台汇总评语自动生成（P1/P2）

- **定位**：总台 `org-admin.js` / `master-hub.js` 汇总各子台数据后，自动生成对比与评语。
- **提升效率**：省去人脑汇总、逐台写评语。
- **实现思路**：`fetchAllMembersData()` 已能拉全量，新增 LLM 汇总生成各子台小结 + 横向对比 + 管理建议（可复用 F3 的解读逻辑）。
- **数据/接口条件**：LLM API；`masterHub.fetchAllMembersData`。
- **成本风险**：低。

### F10 自然语言问答 / 智能报表（P2）

- **定位**：全局问答入口，如「本月哪个指标最差」「哪些班主任续费率异常」。
- **提升效率**：从「翻多个视图找数据」到「一句话出结论」。
- **实现思路**：LLM 读取结构化上下文（`App.viewData` 摘要 + schema 说明），采用「问题 → 结构化查询 → LLM 归纳」两步；初期用数据摘要而非全量避免 token 爆炸。
- **数据/接口条件**：LLM API；需维护一份数据 schema 说明与指标词典。
- **成本风险**：中（上下文管理 + 准确性需打磨）。

### F11 时间轴节律智能排程（P2）

- **定位**：`timeline.js` 从全手动到半自动生成下周/次月节律。
- **提升效率**：减少手动新建/拖拽节点。
- **实现思路**：L0 基于历史节律周期自动预填下周节点；L1 从会议纪要/周报提取新增节律并建议星期/时间/提醒。
- **数据/接口条件**：`timeline` 历史；L1 需 LLM + 会议纪要文本。
- **成本风险**：低。

### F12 今日优先级智能排序 + 提醒（P2，纯本地）

- **定位**：`today.js` 从硬编码分类到个性化优先级排序 + 实际提醒触发。
- **提升效率**：每天开工即知「最该做什么」。
- **实现思路**：L0 结合逾期/临期/里程碑/黄历宜忌/负责人负载，对今日任务评分排序；补齐 `reminder` 的浏览器通知触发。
- **数据/接口条件**：`tasks` + `timeline` + `teacherMilestones`，无外部依赖。
- **成本风险**：¥0；低。

---

## 四、分阶段实施建议

### 阶段 0：AI 地基（1 个迭代）
- 新增 `js/ai.js`（LLM 调用统一封装：Key 读取、脱敏开关、超时/降级/限流、JSON 解析校验）与 `js/ai-local.js`（统计预测/异常检测/规则化生成库）。
- 「设置 → AI」页：Key 填写、脱敏开关、模型选择、启用开关。
- 统一「AI 洞察」UI 组件与设计令牌（沿用 Linear 风格）。
- **预期**：AI 能力可插拔、安全可控，为后续所有功能提供底座。

### 阶段 1：零成本本地智能（先快速见效，2–3 个迭代）
- 落地 **F4（异常检测+趋势预测）**、**F8（项目组健康度）**、**F12（今日优先级+提醒）**、**F5 的 L0 部分（C 值建议区间）**。
- **预期**：不花一分钱、不碰隐私，即获得「自动预警 + 趋势预判 + 催办清单 + 个性化排序」，数据解读从纯人工迈入半自动。

### 阶段 2：LLM 内容生成（核心提效，2–3 个迭代）
- 落地 **F1（周报智能生成）**、**F3（红绿灯解读）**、**F2（粘贴语义解析）**。
- 接入用户自填 Key（方案 A），默认开启脱敏。
- **预期**：周日周报、周二拆解下发、月报文字分析三大高频痛点被自动化，工时显著下降。

### 阶段 3：预测与自动闭环（1–2 个迭代）
- 落地 **F7（教师画像+预警）**、**F9（子台汇总评语）**、**F6（导入智能辅助）**、**F11（节律排程）**。
- **预期**：形成「数据 → 洞察 → 建议 → 行动」的自动闭环，管理从「记录型」升级为「预测型」。

### 阶段 4（可选）：自然语言问答
- 落地 **F10**，待前序数据 schema 与指标词典稳定后再做，避免过早投入。

---

## 五、预期效果与衡量指标

| 指标 | 现状 | 目标（阶段 2 后） |
|---|---|---|
| 周日周报耗时 | 1–2 小时手写 | ≤ 10 分钟审阅微调 |
| 粘贴任务解析成功率 | 规则覆盖约 60–70% | ≥ 90%（LLM 兜底） |
| 数据文字分析 | 纯人工 | 一键生成 + 微调 |
| 异常发现时效 | 人眼被动发现 | 系统主动告警 |
| 数据导入成功率 | 模板不一致即失败 | 自动识别 + 诊断后 ≥ 90% |
| 数据安全 | 本地 | 本地打底 + 可选脱敏出网 |

---

## 六、风险与边界

1. **数据出网**：默认提供「脱敏」开关，教师姓名等先替换为代号再送 LLM；用户可一键关闭 L1 层退回纯本地。
2. **Key 安全**：不落 Git 仓库，存本机 localStorage（方案 A）；后续可平滑迁移 Edge Function 代理（方案 B）。
3. **LLM 幻觉**：所有 LLM 输出仅作「建议/草稿」，最终以人工确认落库，不做自动写入关键数据。
4. **成本**：L0 恒零成本；L1 按 token 计费（单次周报级调用约几分钱量级）。
5. **架构约束**：不改 campus-analytics；所有能力落在 DOS 工作台侧；不破坏现有数据模型与 localStorage/Supabase 结构。
6. **反向联动禁止**：DOS 独有数据（timeline/projects/tasks）不回推分析台。

---

## 七、待确认事项（可按需调整）

1. 技术路线是否采纳「混合方案」（默认：是）。
2. LLM 供应商选择（DeepSeek / 通义千问 / 其他 OpenAI 兼容端点）。
3. 数据出网策略（默认：脱敏后出网）。
4. 阶段 2 是否立即接入真实 Key，还是先只跑阶段 1 本地智能观察效果。
5. 教师 tkpi 联动的姓名匹配键约定（F7 前置依赖）。
