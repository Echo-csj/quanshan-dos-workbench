# 数据工作台 × 个人工作台 · 联动优化建议

> 产出时间：2026-08-27　|　范围：仅调整个人工作台（DOS）数据看板，数据分析工作台（campus-analytics）内容保持不变（硬性约束）。

---

## 一、背景与现状（已实现部分）

**数据流**：campus-analytics 通过 Supabase `shared_link` 表 upsert 一份 `analytics_snapshot`，结构为 `{ generatedAt, totalRecords, latestByStream: { [stream]: 最新记录 } }`。DOS 的 `sync.js` 登录后调用 `App.sync.readShared()` 拉取该快照。

**已落地**：本次已在 DOS 数据看板新增「🔗 联动数据」Tab（`js/views/linked-data.js`），自动提取快照中 `weekly` 流的字段，通过 `CAMPUS_TO_DOS` 映射（16 个核心指标）复用 DOS 既有指标口径（标签/基准/红绿灯），并用 `ResizeObserver` 动态网格实现「空间大小合理、视觉美观」的自适应布局。

**关键洞察**：`buildSnapshot()` 会把 campus 端**每一个 stream** 的最新记录都塞进 `latestByStream`（weekly / monthly / bestkezu / kezuActual / tkpi 统统在内）。也就是说，**更多联动数据其实已经躺在快照里，只是 DOS 目前只读了 `weekly`**。下面的优化全部是「DOS 侧多读几个字段」，**不需要改动 campus-analytics 任何内容**。

---

## 二、两工作台数据结构对比

| 数据域 | 数据分析工作台（campus-analytics） | 个人工作台（DOS）现状 | 联动状态 |
|---|---|---|---|
| 周报课时/生产/续费/退费/结课/停课/人力 | `stream=weekly`（values 全量字段） | 数据看板·🔗联动数据（Tab 已实现动态布局） | ✅ 已联动 |
| 月度数据（月末周报） | `stream=monthly` | `reports.monthly` 仅本地 Excel 导入，未引用快照 | ⬜ 未利用 |
| 教师月度台账 KPI | `stream=tkpi`（教师×月：学员数/课次/续费/结课/退费/停课人数，率值读取时派生） | `teachers` 模块（标签/状态，无量化 KPI 联动） | ⬜ 未联动 |
| 五项满意度 | 由 `monthly` 派生（`satisfactionItems`） | 无满意度模块 | ⬜ 未联动 |
| 科组跟踪 | `stream=bestkezu` / `kezuActual` | 无科组概念 | ⬜ 未联动 |
| 季度/年度汇总 | 由 `monthly` 派生 | `reports.monthly` 本地汇总 | ⚠️ 双源并存 |
| 周计划/月安排/6项目组/任务 | 无 | `timeline` / `projects` / `tasks`（DOS 独有，核心差异） | — |

**结论**：可联动的「数据源」集中在 campus 的 `monthly`、`tkpi`、`bestkezu` 三个 stream；DOS 独有（timeline/projects/tasks）反向推送 campus 受约束禁止，故不纳入。

---

## 三、可联动数据识别 & 落地建议（全部仅改 DOS）

按「工作量小、价值直接」排序。

### P0｜月度对比（MoM）— 工作量最小，数据已在快照

- **现状**：快照已含 `monthly` 最新记录，但 `linked-data.js` 只解析了 `weekly`。
- **建议**：在「🔗 联动数据」Tab 增加「月度快照」区块，展示当月月末周报核心指标，并计算环比（本月 vs 上月、周 vs 月）。
- **落地方式**：在 `linked-data.js` 的 `buildHTML()` 中新增读取 `snap.latestByStream['monthly']`，复用现有 `cellInfo()` / `FEATURES` 与动态网格。**无需改 campus。**
- **风险**：极低（纯读取 + 渲染）。

### P1｜教师月度台账 `tkpi` → DOS `teachers` 联动 — 中工作量

- **现状**：DOS `teachers` 靠标签维护「在职/待离职/ 离职」，缺乏量化 KPI；campus `tkpi` 已沉淀每位教师每月的学员数、课次、续费/结课/退费/停课人数（率值可在 DOS 侧按 `率 = 人数 / v1Students` 现算）。
- **建议**：`teachers` 视图增加「联动 KPI」列或弹层，按**教师姓名**匹配 `tkpi` 最新月记录，展示其量化表现，杜绝重复录入。
- **落地方式**：新建 `js/views/teachers-link.js`（或从 `linkedData` 暴露 `getLinkedTkpi(teacherName)`），在 `teachers.render` 中按需调用；匹配键需约定（两端教师姓名完全一致）。`tkpi` 派生率（续费/结课/退费/停课率、月饱和度、月度周平均）的公式在 campus `aggregate.js` 的 `tkpiMonthDerived` 中，DOS 侧可照抄实现（因不改 campus，只能 DOS 侧复算）。
- **风险**：中（需约定姓名匹配键 + 复算派生率）。**无需改 campus 字段。**

### P1｜五项满意度 → 数据看板新增「满意度」卡 — 中工作量

- **现状**：campus 从 `monthly` 提取五项满意度（`satisfactionItems`：续费/结课/退费/停课单科率、推荐单科率），DOS 无。
- **建议**：在「🔗 联动数据」Tab 新增「满意度」分组卡，读取 `monthly.values` 中对应 `src` 字段，并可叠加 DOS 基线对照。
- **落地方式**：在 `linked-data.js` 的 `GROUPS` 追加「满意度」组，字段取 campus `SCHEMA.satisfactionItems` 的 `src`（均为 `xfMonthSubjRate / jkMonthSubjRate / tfMonthSubjRate / tkNumRate / tjMonthSubjRate`，这些键已存在于 `weekly`/`monthly` 的 values 中）。**无需改 campus。**
- **风险**：低。

### P2｜入离职·请假 → DOS `hr` 自动预填 —— 低工作量（已部分呈现）

- **现状**：「🔗 联动数据」已有「入离职·请假」分组卡（周/月离职人数率、请假率、加课等）；DOS `hr` 模块目前独立登记。
- **建议**：将联动数据中的 `quitMonthRate` / `leaveRate` 作为 `hr` 模块默认值或只读参考，减少重复录入与口径冲突。
- **落地方式**：在 `hr` 视图加载时调用 `App.sync.readShared()` 取 `weekly` 对应字段做默认值/提示。**无需改 campus。**

### P2｜科组 `bestkezu` → 参考面板 —— 中工作量

- **现状**：campus 科组月度跟踪（`subjects/hours/weekAvg/jkSubj/tfSubj/...`）已沉淀在 `bestkezu`；DOS 无科组概念。
- **建议**：在「🔗 联动数据」Tab 增加「科组参考」只读卡，便于 DOS 侧做横向对比。
- **落地方式**：读取 `snap.latestByStream['bestkezu']`，按 `kezuFields` 渲染。**无需改 campus。**

---

## 四、明确边界（不可行 / 受限部分）

1. **反向联动禁止**：DOS 独有数据（timeline 周计划、projects 6 项目组、tasks）推回 campus-analytics **违反硬性约束**，不实施。
2. **campus 零改动**：所有扩展仅在 DOS 解析快照时「多读几个 stream / 多算几个派生值」，campus 的字段、聚合、UI 一律不动。
3. **派生值复算**：campus 的季度/年度汇总、满意度、tkpi 率值均为「读取时派生、不落库」。DOS 若想展示这些，需在 DOS 侧复刻公式（已在 P1 注明），不能要求 campus 额外落库。

---

## 五、推荐落地顺序

| 优先级 | 项目 | 工作量 | 价值 | 是否需改 campus |
|---|---|---|---|---|
| **P0** | 月度对比（MoM） | 小 | 直接（周/月双口径） | 否 |
| **P1** | 教师台账 tkpi 联动 | 中 | 高（消灭重复录入） | 否（DOS 侧复算率） |
| **P1** * | 五项满意度卡 | 中 | 中（补全指标） | 否 |
| **P2** | 入离职请假 → hr 预填 | 小 | 中 | 否 |
| **P2** | 科组参考面板 | 中 | 低-中 | 否 |

**建议下一步**：先落地 **P0（月度对比）**，工作量最小、价值最直接；随后推进 **P1 教师台账联动**。两者均为纯 DOS 侧改动，完全满足「数据分析台内容保持不变」的约束。

---

### 附：已交付代码改动清单（本次）

| 文件 | 改动 |
|---|---|
| `js/views/linked-data.js`（新增） | 自动提取 `weekly` 快照、字段映射、动态网格布局、核心卡 + 6 分组展示 |
| `js/views/data.js` | 数据看板新增 `linked` Tab（🔗 联动数据）并路由渲染 |
| `js/sync.js` | 登录成功后派发 `dos:linked-update`，联动模块自动刷新 |
| `index.html` | 引入 `linked-data.js`，相关脚本 `?v=` 缓存破坏至 `20260827a` |
| `styles.css` | 新增 `.linked-*` 样式（核心卡/分组卡/芯片/状态点），沿用既有设计令牌 |

> 约束核验：campus-analytics 未做任何改动；所有变更均落在个人工作台（DOS）数据看板内。
